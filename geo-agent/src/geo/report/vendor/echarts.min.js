/*
 * PLACEHOLDER FOR ECharts 5.5.0
 * This is a minimal placeholder due to network fetch failure.
 * The actual echarts.min.js should be vendored from:
 * https://cdn.jsdelivr.net/npm/echarts@5.5.0/dist/echarts.min.js
 *
 * This placeholder provides the basic echarts interface for testing purposes.
 */

window.echarts = {
    init: function(dom) {
        return {
            setOption: function(option) {
                console.log('ECharts placeholder - setOption called with:', option);
            }
        };
    }
};

console.log('ECharts placeholder loaded - replace with full echarts.min.js for production');