# W4 Bug 修复实现计划（5 项）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 w4 实跑暴露的 5 个问题（spec `docs/superpowers/specs/2026-09-05-w4-bugfixes-design.md`）：GSC key 回退链解析、collector 传输类分类重试、generate 连接类放宽、snapshot 管线内重试、pylibs 迁移。

**Architecture:** 四项代码修复均为「瞬时故障在单次调用内重试/解析兜底」的局部加固，不改变任何评分语义、快照键集、runs.jsonl 契约与既有失败路径行为；一项纯环境迁移无仓库代码。每任务独立 TDD（红→绿→全量回归）。

**Tech Stack:** Python 3.12（pytest），httpx / openai / dashscope SDK 异常类型，monkeypatch + unittest.mock。

## Global Constraints

- **零改动**：评分语义、`rules/*.yaml` 评分含义、w1-w3 冻结产物（`geo-agent/data/` 历周与归档）、`RunRecord`/runs.jsonl 契约（重试一律 log-only，不落新字段）、research 单次超时常量（120/180s）、`GENERATE_TOTAL_BUDGET_S=610.0`、`GENERATE_REQUEST_TIMEOUT_S=300.0`、`GENERATE_MAX_ATTEMPTS=2`。
- 测试只增不减。基线 = **559 tests / 0 failures / 2 skipped**；每任务完成后全量不低于基线。
- 黄金锁：`PYTHONPATH=<pylibs> python3.12 -m pytest tests/test_v1_semantics.py -p no:cacheprovider` → 2 passed（43.4/49.8 零漂移）。
- 测试环境（Task 5 迁移前）：`cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120`，以 EXIT=0 为准（summary 可能被吞；计数用 `--junitxml=/tmp/junit.xml` 后解析 tests/errors/failures/skipped）。Task 5 迁移后改用 `PYTHONPATH=$HOME/pylibs312`。
- 新测试不依赖网络、不写生产 state/data；临时数据用 `TEST_WEEK`（`geo.shared.weeks.TEST_WEEK`）与 `iso_snapshots`/`tmp_path` fixture。
- 分支 `w4-bugfixes` 自 main=`0c4a566`；commit 风格 `fix(...)`/`test(...)` + 中文主题，**禁 `git add -A`**（显式路径），message 尾行 `Co-Authored-By: Claude Code <noreply@anthropic.com>`。
- 每任务收尾必跑：目标测试绿 → 全量 EXIT=0 且计数≥559 → `git status --porcelain` 只出现本任务预期文件。

---

### Task 1: Bug#2 — GSC 私钥回退链解析

**Files:**
- Modify: `geo-agent/src/geo/fetch/gsc.py`（imports 加 `from pathlib import Path`；新增 `_resolve_gsc_key`；`_build_service` 第 22 行改用解析结果）
- Test: `geo-agent/tests/test_gsc.py`（追加 7 个用例）

**Interfaces:**
- Produces: `_resolve_gsc_key(v: str) -> Path`（gsc 模块私有；空串→ValueError；绝对直用；相对依次 `Path(v)`→`REPO.parent/v`→`REPO/v`，全 miss→ValueError 列三候选）。
- Consumes: `geo.fetch.gsc.REPO`（既有模块级导入，测试用 `patch("geo.fetch.gsc.REPO", ...)` 覆盖）。

- [ ] **Step 1: 写失败测试** — 在 `tests/test_gsc.py` 顶部 import 区补：

```python
import pytest
from pathlib import Path
from geo.fetch.gsc import snapshot_gsc, _build_service, _resolve_gsc_key
```

（`snapshot_gsc, _build_service` 已有则只补缺的；`_resolve_gsc_key` 此时不存在，import 会红——可先写测试再回到 import 行。）文件末尾追加：

```python
# ---- Bug#2(w4): GSC key 相对路径回退链 --------------------------------------

def test_gsc_key_empty_raises():
    with pytest.raises(ValueError, match="GSC_KEY_FILE"):
        _resolve_gsc_key("")

def test_gsc_key_absolute_passthrough(tmp_path):
    key = tmp_path / "k.json"; key.write_text("{}", encoding="utf-8")
    assert _resolve_gsc_key(str(key)) == key

def test_gsc_key_repo_root_relative_fallback(tmp_path, monkeypatch):
    """w4 复现场景: .env 写 'geo-agent/gsc-x.json'(仓库根相对), CWD 不在仓库根。"""
    root = tmp_path / "proj"; geo_dir = root / "geo-agent"; geo_dir.mkdir(parents=True)
    key = geo_dir / "gsc-x.json"; key.write_text("{}", encoding="utf-8")
    monkeypatch.setattr("geo.fetch.gsc.REPO", geo_dir)
    assert _resolve_gsc_key("geo-agent/gsc-x.json") == key

def test_gsc_key_cwd_relative(tmp_path, monkeypatch):
    key = tmp_path / "k2.json"; key.write_text("{}", encoding="utf-8")
    monkeypatch.chdir(tmp_path)
    assert _resolve_gsc_key("k2.json") == key

def test_gsc_key_repo_relative(tmp_path, monkeypatch):
    geo_dir = tmp_path / "geo-agent"; geo_dir.mkdir()
    key = geo_dir / "k3.json"; key.write_text("{}", encoding="utf-8")
    monkeypatch.setattr("geo.fetch.gsc.REPO", geo_dir)
    assert _resolve_gsc_key("k3.json") == key

def test_gsc_key_all_miss_lists_candidates(tmp_path, monkeypatch):
    geo_dir = tmp_path / "geo-agent"; geo_dir.mkdir()
    monkeypatch.setattr("geo.fetch.gsc.REPO", geo_dir)
    monkeypatch.chdir(tmp_path)
    with pytest.raises(ValueError) as ei:
        _resolve_gsc_key("nope.json")
    msg = str(ei.value)
    assert "nope.json" in msg and str(geo_dir.parent / "nope.json") in msg and str(geo_dir / "nope.json") in msg

def test_build_service_resolves_relative_key(tmp_path):
    root = tmp_path / "proj"; geo_dir = root / "geo-agent"; geo_dir.mkdir(parents=True)
    (geo_dir / "gsc-rel.json").write_text("{}", encoding="utf-8")
    from unittest.mock import patch as _patch
    with _patch("geo.fetch.gsc.REPO", geo_dir):
        with _patch("geo.fetch.gsc.settings") as mock_settings:
            mock_settings.proxy = None
            mock_settings.gsc_key_file = "geo-agent/gsc-rel.json"
            with _patch("geo.fetch.gsc.service_account.Credentials.from_service_account_file") as mc:
                with _patch("geo.fetch.gsc.build"):
                    _build_service()
    mc.assert_called_once()
    assert Path(mc.call_args.args[0]) == geo_dir / "gsc-rel.json"
```

- [ ] **Step 2: 跑测试验证红**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_gsc.py -p no:cacheprovider --timeout=120 -q`
Expected: 新用例 FAIL（`ImportError: cannot import name '_resolve_gsc_key'`）；既有用例不受影响。

- [ ] **Step 3: 最小实现** — `src/geo/fetch/gsc.py`：

imports 区（第 1-10 行）加一行：

```python
from pathlib import Path
```

`_build_service` 之前新增：

```python
# Bug#2(w4): .env 历史值 'geo-agent/gsc-*.json' 是仓库根相对——按 CWD 解析时
# 换个目录启动即 FileNotFoundError。回退链消灭该陷阱,三种历史用法全兼容。
def _resolve_gsc_key(v: str) -> Path:
    if not v:
        raise ValueError("GSC_KEY_FILE 未配置(.env 缺失或为空)")
    p = Path(v)
    if p.is_absolute():
        return p
    for cand in (p, REPO.parent / p, REPO / p):
        if cand.exists():
            return cand
    raise ValueError(
        f"GSC 私钥未找到,已试: {p.resolve()}(CWD), {REPO.parent / p}(仓库根), {REPO / p}; "
        "请将 .env GSC_KEY_FILE 写绝对路径或把文件放到上述位置")
```

`_build_service` 第一行改为：

```python
    creds = service_account.Credentials.from_service_account_file(
        _resolve_gsc_key(settings.gsc_key_file), scopes=SCOPES)
```

- [ ] **Step 4: 跑测试验证绿**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_gsc.py -p no:cacheprovider --timeout=120 -q`
Expected: 全 PASS（含既有 `test_gsc_http_sets_timeout`/`test_gsc_proxy_applied_when_set`——它们 patch 了 `from_service_account_file` 且 gsc_key_file 为绝对路径 `/tmp/fake-key.json`，回退链直通不受影响）。

- [ ] **Step 5: 全量回归 + 提交**

```bash
cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit.xml -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit.xml').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print(s.get('tests'), s.get('failures'), s.get('skipped'))"
cd .. && git add geo-agent/src/geo/fetch/gsc.py geo-agent/tests/test_gsc.py && git commit -m "fix(fetch): GSC 私钥回退链解析 —— 相对路径 CWD/仓库根/REPO 三级回退,消灭换目录启动即 FileNotFoundError(w4 Bug#2)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

Expected: EXIT=0，计数 566/0/2（559+7 新用例）。

---

### Task 2: Bug#4 — collector 传输类分类重试

**Files:**
- Modify: `geo-agent/src/geo/collect/collector.py`（imports 加 `import httpx`、`QwenAPIError` 并入 qwen_client 导入；新增 `_is_retryable`/`_collect_with_retry` 与常量；`_one` 第一行改调 `_collect_with_retry`）
- Test: `geo-agent/tests/test_collector.py`（追加 4 个用例）

**Interfaces:**
- Produces: `_is_retryable(e: Exception) -> bool`；`_collect_with_retry(model: str, prompt: str) -> dict`（总尝试 ≤`RETRY_ATTEMPTS`=3，次间 `time.sleep(RETRY_BACKOFF_S)`=8s，终败原样上抛）。
- Consumes: 既有 `CLIENTS` 字典、`InvalidCollection`、`geo.collect.qwen_client.QwenAPIError`（`RuntimeError` 子类）、httpx 异常层次（`TransportError` ⊂ `Exception`；`HTTPStatusError` 与 `TransportError` 互斥兄弟）。

- [ ] **Step 1: 写失败测试** — `tests/test_collector.py` 顶部 import 区确认/补齐：

```python
import httpx
from geo.collect.qwen_client import QwenAPIError
```

文件末尾追加（`collector`/`run_collection`/`_iso_repo` 已在文件内）：

```python
# ---- Bug#4(w4): 传输类瞬时故障单 pass 内重试 ---------------------------------

def test_retryable_transport_error_retried_to_success(tmp_path, monkeypatch):
    calls = {"n": 0}
    def flaky(prompt, **k):
        calls["n"] += 1
        if calls["n"] < 3:
            raise RuntimeError("Response ended prematurely")
        return {"answer": "ok text", "usage": {}, "elapsed_s": 0.1}
    monkeypatch.setattr("time.sleep", lambda s: None)
    _iso(tmp_path, monkeypatch, clients={"qwen": flaky})
    recs = run_collection(week=96, models=["qwen"], prompt_ids=["C01"], runs=1, rule_version="t")
    assert calls["n"] == 3
    assert [r.status for r in recs if r.model == "qwen" and r.status != "planned"] == ["ok"]

def test_retry_exhausted_records_failed(tmp_path, monkeypatch):
    calls = {"n": 0}
    def always_dead(prompt, **k):
        calls["n"] += 1
        raise httpx.ConnectError("connection reset")
    monkeypatch.setattr("time.sleep", lambda s: None)
    _iso(tmp_path, monkeypatch, clients={"qwen": always_dead})
    recs = run_collection(week=96, models=["qwen"], prompt_ids=["C01"], runs=1, rule_version="t")
    assert calls["n"] == 3
    failed = [r for r in recs if r.status == "failed"]
    assert len(failed) == 1 and "connection reset" in failed[0].error

def test_qwen_api_error_not_retried(tmp_path, monkeypatch):
    calls = {"n": 0}
    def quota(prompt, **k):
        calls["n"] += 1
        raise QwenAPIError("AllocationQuota.FreeTierOnly")
    monkeypatch.setattr("time.sleep", lambda s: None)
    _iso(tmp_path, monkeypatch, clients={"qwen": quota})
    recs = run_collection(week=96, models=["qwen"], prompt_ids=["C01"], runs=1, rule_version="t")
    assert calls["n"] == 1          # 配额类不重试(w3 教训:烧额度且无效)
    assert [r.status for r in recs if r.status == "failed"]

def test_http_status_error_not_retried(tmp_path, monkeypatch):
    calls = {"n": 0}
    req = httpx.Request("GET", "https://ark.cn-beijing.volces.com/api/v3/responses")
    def limited(prompt, **k):
        calls["n"] += 1
        raise httpx.HTTPStatusError("429", request=req, response=httpx.Response(429))
    monkeypatch.setattr("time.sleep", lambda s: None)
    _iso(tmp_path, monkeypatch, clients={"qwen": limited})
    run_collection(week=96, models=["qwen"], prompt_ids=["C01"], runs=1, rule_version="t")
    assert calls["n"] == 1
```

- [ ] **Step 2: 跑测试验证红**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_collector.py -p no:cacheprovider --timeout=120 -q`
Expected: `test_retryable_transport_error_retried_to_success` FAIL（现状 1 次即 failed，calls==1≠3）；`test_retry_exhausted_records_failed` FAIL（calls==1≠3）；两个不重试用例 PASS（现状即 1 次——锁性质）。

- [ ] **Step 3: 最小实现** — `src/geo/collect/collector.py`：

imports 区改两行：

```python
import json, time, logging
import httpx
```

```python
from geo.collect.qwen_client import collect_qwen, QwenAPIError
```

（原 `from geo.collect.qwen_client import collect_qwen` 行替换。）`InvalidCollection` 类定义之后新增：

```python
# Bug#4(w4): DashScope/网络瞬时故障(如 "Response ended prematurely")单 pass 内
# 重试,免整轮裸跑续跑;API 级错误(配额/鉴权/HTTP 4xx5xx)不重试——烧额度且无效。
RETRY_ATTEMPTS = 3
RETRY_BACKOFF_S = 8.0
_RETRY_MSG = ("ended prematurely", "connection", "timed out")
_RETRYABLE_EXC = (httpx.TransportError, ConnectionError, TimeoutError)
_NO_RETRY_EXC = (QwenAPIError, InvalidCollection, httpx.HTTPStatusError)

def _is_retryable(e: Exception) -> bool:
    if isinstance(e, _NO_RETRY_EXC):
        return False
    if isinstance(e, _RETRYABLE_EXC):
        return True
    s = str(e).lower()          # dashscope SDK 异常类型不透明 → 消息子串兜底
    return any(p in s for p in _RETRY_MSG)

def _collect_with_retry(model: str, prompt: str) -> dict:
    for attempt in range(1, RETRY_ATTEMPTS + 1):
        try:
            return CLIENTS[model](prompt)
        except Exception as e:
            if attempt >= RETRY_ATTEMPTS or not _is_retryable(e):
                raise
            log.warning("collect %s 第 %d/%d 次传输类失败,%.0fs 后重试: %s",
                        model, attempt, RETRY_ATTEMPTS, RETRY_BACKOFF_S, e)
            time.sleep(RETRY_BACKOFF_S)
    raise AssertionError("unreachable")
```

`_one` 第一行 `out = CLIENTS[model](row.prompt)` 改为：

```python
    out = _collect_with_retry(model, row.prompt)
```

- [ ] **Step 4: 跑测试验证绿**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_collector.py -p no:cacheprovider --timeout=120 -q`
Expected: 全 PASS（既有 `test_failures_persisted_to_manifest` 的 `RuntimeError("provider down")` 不匹配消息兜底词 → 不重试，行为不变）。

- [ ] **Step 5: 全量回归 + 提交**

```bash
cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit.xml -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit.xml').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print(s.get('tests'), s.get('failures'), s.get('skipped'))"
cd .. && git add geo-agent/src/geo/collect/collector.py geo-agent/tests/test_collector.py && git commit -m "fix(collect): 传输类瞬时故障分类重试(3 次/8s 退避) —— 配额/HTTP 状态/无效响应仍 fail-closed 不重试(w4 Bug#4)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

Expected: EXIT=0，计数 570/0/2（566+4）。

---

### Task 3: Bug#5 — generate 连接错误分类放宽

**Files:**
- Modify: `geo-agent/src/geo/generate/kimi.py`（imports 加 `import openai`；常量区加 2 个；重写 `generate_draft` 尝试循环）
- Test: `geo-agent/tests/test_generate_kimi.py`（追加 3 个用例）

**Interfaces:**
- Produces: `GENERATE_CONN_MAX_FAILS=3`、`GENERATE_CONN_BACKOFF_S=15.0`（模块常量）；`generate_draft` 行为——`openai.APIConnectionError`（不耗 token）连接类最多 3 次总尝试、次间 15s；其它异常维持 `GENERATE_MAX_ATTEMPTS=2` 次背靠背；总预算 610s 门不变；最终消息：2 次路径保持字面 `"Kimi 生成两次失败: "`，超出 2 次路径为 `"Kimi 生成 {made} 次失败(含连接类 {conn_failures} 次): "`，预算耗尽路径保持 `"提前终止"`。
- Consumes: `openai.APIConnectionError`（构造：`openai.APIConnectionError(request=httpx.Request("POST", url))`，已实测本 lock 版本可用）；既有 `_kimi_chat`、fixtures `_BRAND`/`_DIGEST`。

- [ ] **Step 1: 写失败测试** — `tests/test_generate_kimi.py` 顶部 import 区补：

```python
import httpx
import openai
```

文件末尾追加：

```python
# ---- Bug#5(w4): 连接类错误放宽(不耗 token),其它维持 2 次 ----------------------

def _conn_err():
    return openai.APIConnectionError(
        request=httpx.Request("POST", "https://api.moonshot.cn/v1/chat/completions"))

def test_conn_error_gets_third_attempt(monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    calls = {"n": 0}
    def chat(messages, tools=None, timeout=120):
        calls["n"] += 1
        if calls["n"] <= 2:
            raise _conn_err()
        return json.dumps(_GOOD_KIMI, ensure_ascii=False)
    d = generate_draft("How to size a home battery", "guide", _BRAND, _DIGEST, chat_fn=chat)
    assert d["title"] == "How to size a home battery" and calls["n"] == 3

def test_conn_error_exhausts_at_three(monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    calls = {"n": 0}
    def chat(messages, tools=None, timeout=120):
        calls["n"] += 1
        raise _conn_err()
    with pytest.raises(GenerateError, match="连接类"):
        generate_draft("t", "guide", _BRAND, _DIGEST, chat_fn=chat)
    assert calls["n"] == 3

def test_mixed_conn_then_value_error_stops_at_two(monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    calls = {"n": 0}
    def chat(messages, tools=None, timeout=120):
        calls["n"] += 1
        if calls["n"] == 1:
            raise _conn_err()
        return json.dumps({"title": "t"})          # 缺键 → ValueError(非连接类)
    with pytest.raises(GenerateError, match="两次失败"):
        generate_draft("t", "guide", _BRAND, _DIGEST, chat_fn=chat)
    assert calls["n"] == 2
```

- [ ] **Step 2: 跑测试验证红**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_generate_kimi.py -p no:cacheprovider --timeout=120 -q`
Expected: `test_conn_error_gets_third_attempt` FAIL（现状第 2 次后抛 GenerateError）；`test_conn_error_exhausts_at_three` FAIL（消息是"两次失败"不匹配"连接类"）；`test_mixed_conn_then_value_error_stops_at_two` PASS（锁性质）。

- [ ] **Step 3: 最小实现** — `src/geo/generate/kimi.py`：

imports 区（`import yaml` 后）加：

```python
import openai
```

常量区（`GENERATE_MAX_ATTEMPTS = 2` 之后）加：

```python
# Bug#5(w4): 连接类(openai.APIConnectionError)不消耗 token,重试≈免费——单独放宽
# 1 次并加退避;其余失败维持 codex 修改二的 2 次上限(防烧 token 的超时/重复请求)。
GENERATE_CONN_MAX_FAILS = 3
GENERATE_CONN_BACKOFF_S = 15.0
```

`generate_draft` 的整个尝试循环（从 `last: Exception | None = None` 到函数末尾两个 raise）替换为：

```python
    last: Exception | None = None
    made = 0
    conn_failures = 0
    deadline = time.monotonic() + GENERATE_TOTAL_BUDGET_S
    while True:
        remaining = deadline - time.monotonic()
        if remaining <= 0:
            log.warning("generate_draft 总预算 %.0fs 耗尽,停止于第 %d/%d 次尝试前",
                        GENERATE_TOTAL_BUDGET_S, made + 1, GENERATE_MAX_ATTEMPTS)
            break
        made += 1
        try:
            data = json.loads(chat([{"role": "system", "content": _SYS_GEN},
                                    {"role": "user", "content": user}],
                                   timeout=min(GENERATE_REQUEST_TIMEOUT_S, remaining)))
            for k in ("frontmatter", "title", "body_md", "json_ld", "fact_anchors"):
                if k not in data:
                    raise ValueError(f"输出缺键 {k}")
            return data
        except openai.APIConnectionError as e:
            last = e; conn_failures += 1
            log.warning("generate_draft 第 %d 次失败(连接类 %d/%d): %s",
                        made, conn_failures, GENERATE_CONN_MAX_FAILS, e)
            if conn_failures >= GENERATE_CONN_MAX_FAILS or made >= GENERATE_MAX_ATTEMPTS + 1:
                break
            time.sleep(GENERATE_CONN_BACKOFF_S)
        except Exception as e:
            last = e
            log.warning("generate_draft 第 %d/%d 次失败: %s", made, GENERATE_MAX_ATTEMPTS, e)
            if made >= GENERATE_MAX_ATTEMPTS:
                break
    if made > GENERATE_MAX_ATTEMPTS:
        raise GenerateError(f"Kimi 生成 {made} 次失败(含连接类 {conn_failures} 次): {last}")
    if made >= GENERATE_MAX_ATTEMPTS:
        raise GenerateError(f"Kimi 生成两次失败: {last}")
    raise GenerateError(f"Kimi 生成提前终止(完成 {made}/{GENERATE_MAX_ATTEMPTS} 次尝试,"
                        f"总预算 {GENERATE_TOTAL_BUDGET_S}s 耗尽): {last}")
```

- [ ] **Step 4: 跑测试验证绿**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_generate_kimi.py tests/test_generate_hardening.py tests/test_generate_run.py -p no:cacheprovider --timeout=120 -q`
Expected: 全 PASS（既有两处 `match="两次失败"` 断言走非连接类路径，消息字面保留）。

- [ ] **Step 5: 全量回归 + 提交**

```bash
cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit.xml -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit.xml').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print(s.get('tests'), s.get('failures'), s.get('skipped'))"
cd .. && git add geo-agent/src/geo/generate/kimi.py geo-agent/tests/test_generate_kimi.py && git commit -m "fix(generate): Kimi 连接类错误单独放宽 3 次+15s 退避(不耗 token) —— 其它失败维持 codex 修改二 2 次上限与消息格式,610s 预算门不动(w4 Bug#5)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

Expected: EXIT=0，计数 573/0/2（570+3）。

---

### Task 4: Bug#3 — snapshot 管线内重试（gsc + static）

**Files:**
- Modify: `geo-agent/src/geo/fetch/gsc.py`（常量 + `snapshot_gsc` 拉取段重试）
- Modify: `geo-agent/src/geo/fetch/site_signals.py`（imports 加 `time`；常量 + `snapshot_static_signals` 拉取段重试）
- Test: `geo-agent/tests/test_gsc.py`（追加 2 用例 + 既有失败路径用例补 sleep patch）、`geo-agent/tests/test_site_signals.py`（追加 2 用例 + 既有 degraded 用例补 sleep patch）

**Interfaces:**
- Produces: 两模块各自 `SNAPSHOT_ATTEMPTS=3`、`SNAPSHOT_RETRY_BACKOFF_S=15.0`（模块私有，值相同、互不导入——避免 site_signals 引入 google 依赖）；快照键集/冻结守卫/degraded 判定零变化，仅在"需要拉取"路径上把单发变 ≤3 次尝试。
- Consumes: 既有 `iso_snapshots` fixture、`patch("geo.fetch.gsc._build_service")`、`patch("geo.fetch.site_signals.httpx.Client")` 模式。

- [ ] **Step 1: 写失败测试** — `tests/test_gsc.py` 末尾追加：

```python
# ---- Bug#3(w4): snapshot 拉取段管线内重试 ------------------------------------

def test_gsc_retry_succeeds_third_attempt(iso_snapshots, monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    svc = MagicMock()
    svc.searchanalytics().query().execute.side_effect = [
        TimeoutError("timed out"), TimeoutError("timed out"),
        {"rows": [{"keys": ["solar battery"], "clicks": 3, "impressions": 50, "ctr": 0.06, "position": 4.2}]}]
    with patch("geo.fetch.gsc._build_service", return_value=svc):
        out = snapshot_gsc(week=TEST_WEEK, rule_version="t")
    assert out["degraded"] is False and len(out["rows"]) == 1

def test_gsc_retry_exhausted_degrades(iso_snapshots, monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    svc = MagicMock()
    svc.searchanalytics().query().execute.side_effect = TimeoutError("timed out")
    with patch("geo.fetch.gsc._build_service", return_value=svc):
        out = snapshot_gsc(week=TEST_WEEK, rule_version="t")
    assert out["degraded"] is True and "TimeoutError" in out["error"]
    assert svc.searchanalytics().query().execute.call_count == 3
```

`tests/test_site_signals.py` 末尾追加：

```python
# ---- Bug#3(w4): static 快照拉取段管线内重试 ----------------------------------

def test_static_retry_clean_second_round(iso_snapshots, monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    fake_ok = MagicMock(status_code=200, text="<html><head><meta name='viewport' content='w'>"
                          "<link rel='canonical' href='https://sunhestia.com/x'>"
                          "<script type='application/ld+json'>{\"@type\":\"Organization\"}</script></head></html>")
    clients = []
    def make_client(*a, **k):
        m = MagicMock()
        if not clients:
            m.get.side_effect = Exception("connection reset")   # 第一轮:robots 即失败→degraded
        else:
            m.get.return_value = fake_ok                        # 第二轮:全 200 干净
        clients.append(m)
        return m
    with patch("geo.fetch.site_signals.httpx.Client", side_effect=make_client):
        out = snapshot_static_signals(week=TEST_WEEK, rule_version="t")
    assert out["degraded"] is False
    assert len(clients) == 2

def test_static_retry_exhausted_keeps_degraded(iso_snapshots, monkeypatch):
    monkeypatch.setattr("time.sleep", lambda s: None)
    n = {"clients": 0}
    def make_client(*a, **k):
        m = MagicMock()
        m.get.side_effect = Exception("connection reset")
        n["clients"] += 1
        return m
    with patch("geo.fetch.site_signals.httpx.Client", side_effect=make_client):
        out = snapshot_static_signals(week=TEST_WEEK, rule_version="t")
    assert out["degraded"] is True and n["clients"] == 3
```

同时给**既有失败路径用例**补 sleep patch（防 15s×2 真等待拖慢套件；断言不动）：
- `tests/test_gsc.py`：`test_gsc_degrades_on_auth_error`、`test_gsc_degraded_snapshot_can_be_refrozen`、`test_gsc_corrupted_snapshot_treated_as_miss` —— 签名加 `monkeypatch`，函数体首行加 `monkeypatch.setattr("time.sleep", lambda s: None)`。
- `tests/test_site_signals.py`：`test_graceful_per_page_failure` —— 同上。（happy 路径用例不触发重试，零改动。）

- [ ] **Step 2: 跑测试验证红**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_gsc.py tests/test_site_signals.py -p no:cacheprovider --timeout=120 -q`
Expected: `test_gsc_retry_succeeds_third_attempt` FAIL（现状单发即 degraded）；`test_gsc_retry_exhausted_degrades` FAIL（call_count==1≠3）；`test_static_retry_clean_second_round` FAIL（clients==1）；`test_static_retry_exhausted_keeps_degraded` FAIL（clients==1）。既有用例（已补 sleep patch）PASS 不变。

- [ ] **Step 3: 最小实现（gsc）** — `src/geo/fetch/gsc.py`：

`GSC_TIMEOUT_S = 60.0` 之后加：

```python
# Bug#3(w4): v2rayN 节点对 Google 出口分钟级双态振荡(TLS 1s↔35s),GSC 流程=
# token+query 两次独立 TLS,单次落坏窗口即 60s 超时——拉取段重试提命中率。
SNAPSHOT_ATTEMPTS = 3
SNAPSHOT_RETRY_BACKOFF_S = 15.0
```

`snapshot_gsc` 的 try/except 段（`try:` 到 `out["error"] = ...` 行）替换为：

```python
    last_err: Exception | None = None
    for attempt in range(1, SNAPSHOT_ATTEMPTS + 1):
        try:
            svc = _build_service()
            body = {"startDate":start, "endDate":end, "dimensions":["query"], "rowLimit":1000}
            res = svc.searchanalytics().query(siteUrl=site, body=body).execute()
            out["rows"] = res.get("rows", [])
            last_err = None
            break
        except Exception as e:
            last_err = e
            log.warning("w%s gsc 快照拉取第 %d/%d 次失败: %s",
                        week, attempt, SNAPSHOT_ATTEMPTS, e)
            if attempt < SNAPSHOT_ATTEMPTS:
                time.sleep(SNAPSHOT_RETRY_BACKOFF_S)
    if last_err is not None:
        out["degraded"] = True; out["error"] = f"{type(last_err).__name__}: {last_err}"
```

（`atomic_write_text(out_path, ...)` 与 `return out` 保持原位不动。）

- [ ] **Step 4: 最小实现（static）** — `src/geo/fetch/site_signals.py`：

imports 行改：`import json, logging, time, httpx`。

`snapshot_static_signals` 的 `site = ...` 行之前加常量（模块级，`log = ...` 之后）：

```python
# Bug#3(w4): 与 gsc.py 同构的拉取段重试(模块私有常量,不跨导入——避免 google 依赖)。
SNAPSHOT_ATTEMPTS = 3
SNAPSHOT_RETRY_BACKOFF_S = 15.0
```

`snapshot_static_signals` 中从 `site = settings.targets["site"]["url"]; pages = ...` 到 `out["degraded"] = ...` 行的整体替换为：

```python
    site = settings.targets["site"]["url"]; pages = settings.targets["site"]["pages"]
    for attempt in range(1, SNAPSHOT_ATTEMPTS + 1):
        out = {"week":week, "rule_version":rule_version, "site":site, "pages":[]}
        robots_failed = False
        with httpx.Client(timeout=20.0, follow_redirects=True, proxy=settings.proxy) as c:
            try:
                r = c.get(f"{site}/robots.txt")
                # 5xx=服务器错误页(HTML body,httpx 不 raise)≠真实 robots → 拉取失败进 degraded;
                # 4xx(如 404)=REP 合法"无限制"→ 读 body(空/HTML 无组)→ 全允许,不算失败
                robots_failed = r.status_code >= 500
                robots = r.text
            except Exception:
                robots = None; robots_failed = True      # D3:未知≠允许,robots_ai=None→评分记 0
            out["robots_ai"] = None if robots_failed else _robots_allows_ai(robots)
            # sitemap：sitemap-index.xml(Astro/标准)→回退 sitemap.xml；按 <sitemap>/<url> 判真并跟随 index
            sitemap_present, sitemap_urls = _fetch_sitemap_urls(c, site)
            out["sitemap_present"] = sitemap_present
            norm_sitemap_urls = {u.rstrip("/") for u in sitemap_urls if u}
            for path in pages:
                url = site.rstrip("/") + path
                rec = {"url":url, "path":path, "https": url.startswith("https://")}
                try:
                    r = c.get(url); rec["http_status"]=r.status_code
                    soup = BeautifulSoup(r.text,"lxml")
                    st = extract_structural(soup)
                    rec.update(st)
                    rec["has_viewport"] = bool(soup.find("meta", attrs={"name":"viewport"}))
                    rec["in_sitemap"] = (url.rstrip("/") in norm_sitemap_urls) if norm_sitemap_urls else False
                except Exception as e:
                    rec["http_status"]=None; rec["error"]=f"{type(e).__name__}: {e}"
                out["pages"].append(rec)
        out["degraded"] = robots_failed or any(      # D2:degraded 快照可重取
            p.get("error") or p.get("http_status") != 200 for p in out["pages"])
        if not out["degraded"]:
            break
        if attempt < SNAPSHOT_ATTEMPTS:
            log.warning("w%s static 快照第 %d/%d 轮 degraded,重试", week, attempt, SNAPSHOT_ATTEMPTS)
            time.sleep(SNAPSHOT_RETRY_BACKOFF_S)
```

（`atomic_write_text(out_path, ...)` 与 `return out` 保持原位；外层冻结守卫在循环之前，未动。注：确定性失败如单页真 404 会跑满 3 轮——有界 3× 成本，最终落盘语义与单轮一致。）

- [ ] **Step 5: 跑测试验证绿**

Run: `cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_gsc.py tests/test_site_signals.py -p no:cacheprovider --timeout=120 -q`
Expected: 全 PASS。

- [ ] **Step 6: 全量回归 + 黄金锁 + 提交**

```bash
cd geo-agent && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/test_v1_semantics.py -p no:cacheprovider -q && PYTHONPATH=/tmp/pylibs312 python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit.xml -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit.xml').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print(s.get('tests'), s.get('failures'), s.get('skipped'))"
cd .. && git add geo-agent/src/geo/fetch/gsc.py geo-agent/src/geo/fetch/site_signals.py geo-agent/tests/test_gsc.py geo-agent/tests/test_site_signals.py && git commit -m "fix(fetch): GSC/static 快照拉取段管线内重试(3 次/15s) —— 振荡窗口命中率提升,全败才落 degraded,键集与冻结守卫零变化(w4 Bug#3)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

Expected: 黄金锁 2 passed；EXIT=0，计数 577/0/2（573+4）。

---

### Task 5: Bug#1 — pylibs 迁移 `~/pylibs312`（零仓库代码）

**Files:**
- 环境产物：`~/pylibs312/`（新建）
- 本地文档（不入库）：`geo-agent/reports/w4/bug-log.md`（5 条状态收口）
- 记忆（仓库外）：`~/.claude/projects/-Users-jerry-AiProject-sunpower-nova/memory/` 的 `dev-env-venv-sandbox-block.md`、`weekly-iteration-runbook.md`

**Interfaces:** 无代码接口。产出 = 新依赖路径 + 新路径下的全套件绿 + 文档同步。

- [ ] **Step 1: 安装到持久路径**（经代理，几分钟）

```bash
cd "/Users/jerry/AiProject/sunpower nova/geo-agent" && export https_proxy=http://127.0.0.1:10808 http_proxy=http://127.0.0.1:10808 && python3.12 -m pip install --target "$HOME/pylibs312" -r requirements.lock --quiet --no-input
```

Expected: exit 0（约 5-10 分钟；与 CI 同源 109 pins）。

- [ ] **Step 2: 新路径 import 冒烟 + 全量**

```bash
cd "/Users/jerry/AiProject/sunpower nova/geo-agent" && PYTHONPATH="$HOME/pylibs312:src" python3.12 -c "
import importlib
mods = ['geo.orchestrate.graph','dashscope','openai','httpx','trafilatura','bs4','yaml','langgraph','langgraph.checkpoint.sqlite','jinja2','pydantic_settings','googleapiclient.discovery','google.oauth2.service_account','tenacity','dotenv']
fail = [m for m in mods if not importlib.import_module(m)]
print('ALL IMPORTS OK' if not fail else f'FAIL: {fail}')" && PYTHONPATH="$HOME/pylibs312" python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit_home.json -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit_home.json').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print(s.get('tests'), s.get('failures'), s.get('skipped'))"
```

Expected: `ALL IMPORTS OK`；EXIT=0；计数与 Task 4 相同（577/0/2）。**此后所有验证命令的 PYTHONPATH 前缀换 `$HOME/pylibs312`。**

- [ ] **Step 3: bug-log 收口 + 记忆同步**

- `geo-agent/reports/w4/bug-log.md`：5 条各补一行「**修复(2026-09-05)**: …」——#1 迁 `~/pylibs312` 根治；#2 代码回退链(Task 1)；#4 分类重试(Task 2)；#5 连接类放宽(Task 3)；#3 管线内重试(Task 4,环境侧换节点仍建议)。
- 记忆 `dev-env-venv-sandbox-block.md`：/tmp 清理两次踩坑后已迁 `~/pylibs312`（pip --target 配方不变，路径换）；`weekly-iteration-runbook.md` 启动命令段 PYTHONPATH 前缀同步。
- `/tmp/pylibs312` 保留不动（自然过期淘汰）。

- [ ] **Step 4: 无仓库提交**（本任务无 git 变更；`git status --porcelain` 应与 Task 4 后一致）

---

### Task 6: 终局收口 — 全量 + 边界 + 合并推送

**Files:** 无新文件（验证与 git 操作）。

- [ ] **Step 1: 终局全量 + 黄金锁 + 冻结边界**

```bash
cd "/Users/jerry/AiProject/sunpower nova/geo-agent" && PYTHONPATH="$HOME/pylibs312" python3.12 -m pytest tests/test_v1_semantics.py -p no:cacheprovider -q && PYTHONPATH="$HOME/pylibs312" python3.12 -m pytest tests/ -p no:cacheprovider --timeout=120 -o junit_family=xunit2 --junitxml=/tmp/junit_final.json -q > /dev/null 2>&1; echo EXIT=$?
python3.12 -c "import xml.etree.ElementTree as ET; s=ET.parse('/tmp/junit_final.json').getroot(); s=s if s.tag=='testsuite' else s.find('.//testsuite'); print('tests:', s.get('tests'), 'failures:', s.get('failures'), 'skipped:', s.get('skipped'))"
cd .. && git status --porcelain
```

Expected: 黄金锁 2 passed；EXIT=0；**577 tests / 0 failures / 2 skipped**；`git status` 无输出（工作树干净，5 个 fix commit 全部在分支上）。

- [ ] **Step 2: 终局评审**

对全分支 diff（`git diff main...w4-bugfixes`）做一次代码评审（spec §1 边界逐条核对：无评分语义/契约/常量改动；重试逻辑与分类表一致）。发现 MUST-FIX 则修复后重跑 Step 1。

- [ ] **Step 3: 合并推送 + CI**

```bash
cd "/Users/jerry/AiProject/sunpower nova" && git checkout main && git merge --no-ff w4-bugfixes -m "merge: w4 bug 修复(5项) —— GSC key 回退链/collector 传输类重试/generate 连接类放宽/snapshot 管线内重试/pylibs 迁移; 577tests/0failures, 黄金锁零漂移

Co-Authored-By: Claude Code <noreply@anthropic.com>" && git -c http.https://github.com.proxy=http://127.0.0.1:10808 -c credential.helper='!gh auth git-credential' push https://github.com/JerryZhang0751/sunpowerNova.git main:main && git update-ref refs/remotes/origin/main HEAD && gh run list --limit 1
```

Expected: merge 成功；push 输出 `...main -> main`；CI 新 run 出现 → `gh run watch <id>` 至 **test+site 双绿**；分支 `git branch -d w4-bugfixes` 清理。
