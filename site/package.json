{
  "name": "sunhestia-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "SunHestia — residential solar PV + battery storage for European homeowners (GEO experiment site, W1 baseline)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/sitemap": "^3.2.0"
  }
}
